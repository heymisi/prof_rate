backend-build:
  stage: build
  script:
    - cd backend
    - npm install
    - npm run build
backend-tslint:
 stage: lint
 script:
    - cd backend
    - npm i tslint
    - tslint --project .
backend-test:
  stage: test
  script:
    - cd backend
    - npm install
    - npm run test
